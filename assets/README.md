# Assets

- `board.jpg` — place the provided crime-board background image here. The app will load this file if present.
- `board-placeholder.png` — a tiny dark fallback used when `board.jpg` is missing. Replace it with the real background for best results.

Keep file names lowercase to match the paths in the Streamlit app.
