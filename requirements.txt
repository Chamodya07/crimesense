streamlit==1.37.1
pillow==10.1.0
python-docx==1.1.2
reportlab==4.1.0

# core data libs pinned exactly to the versions used during model training
# (this avoids joblib/unpickle errors when scikit-learn schemas change).
# training environment used scikit-learn 1.6.1, joblib 1.5.3, numpy 2.0.2, and
# pandas 2.2.2. ensure your virtual environment matches to load the model.
numpy==2.0.2
pandas==2.2.2
scikit-learn==1.6.1  # must match training version
joblib==1.5.3

# added for NLP transformer inference
transformers>=4.0.0
torch>=2.0.0

# portable tabular model backend
xgboost>=1.7.0

# RAG retrieval dependencies
sentence-transformers>=2.2.2
faiss-cpu>=1.7.3
pyarrow>=11.0.0
kaggle>=1.5.0
firebase-admin>=6.0.0
google-cloud-firestore>=2.11.0
bcrypt>=4.0.0
python-docx==1.1.2
